# Private Endpoint example

This example deploys Azure Managed Redis (Redis Enterprise) with a private endpoint connection.

## Features

- Balanced_B0 SKU (lowest cost tier)
- TLS 1.2 minimum
- EnterpriseCluster clustering policy
- Private endpoint with Private DNS Zone integration
- Public network access disabled
